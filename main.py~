from input_data import *
from output_data import *
import os

folder_path = './corpus/textgrids'
files = []
data = []
for data_file in sorted(os.listdir(folder_path)):
    files.append(data_file)
print(files)
for f in files:
    d = ema_reader('./corpus/textgrids/' + f)
    data.append(d)
    
